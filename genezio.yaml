name: test-github
region: us-east-1
yamlVersion: 2
frontend:
    path: .
    publish: dist
    scripts:
        deploy:
            - npm install
        build:
            - npm run build
        start:
            - npm install --silent
            - npm run dev --silent
    subdomain: github-integration-test
otherStuff: tralala
